<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de los libros</title>

    <link rel="stylesheet" href="/public/style.css">
</head>

<body>
    <section>
        <h1>
            <%= detalles.volumeInfo.title %>
        </h1>
        <div class="detalles">

           <div class="texto">
                <p>
                    <% if (detalles.volumeInfo.authors) { %>
                    Autor/es: <%= detalles.volumeInfo.authors %><br>
                    <% } %>
                    Publicado el <%= detalles.volumeInfo.publishedDate %><br>
                    Categoría/s: <%= detalles.volumeInfo.categories %><br>
                    Sinópsis: <%= detalles.volumeInfo.description %><br>
                </p>
            </div>

            <div class="portada">
                <% if (detalles.volumeInfo.imageLinks && detalles.volumeInfo.imageLinks.thumbnail) { %>
                     <img src="<%= detalles.volumeInfo.imageLinks.thumbnail %>" alt="Portada del libro">
                 <% } else { %>
                    <p>No hay imagen de portada disponible</p>
                <% } %>

                 <% if (detalles.volumeInfo.previewLink) { %>
                    <a href="<%= detalles.volumeInfo.previewLink %>" target="_blank">📙Vista previa</a><br>
                <% } %>
            </div>

        </div>

        <div class="linkInicio">
           <a href="/">Inicio</a>
        </div>

    </section>
</body>

</html>